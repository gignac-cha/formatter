{
  "name": "formatter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install:server": "cd server && npm install",
    "install:ui": "cd ui && npm install",
    "preinstall": "npm run install:server && npm run install:ui",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^2.8.2"
  }
}
